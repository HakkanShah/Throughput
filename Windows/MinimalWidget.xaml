<Window x:Class="Throughput.Windows.MinimalWidget"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Throughput"
        SizeToContent="WidthAndHeight"
        WindowStyle="None"
        AllowsTransparency="True"
        Foreground="White"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        Cursor="SizeAll"
        MouseLeftButtonDown="Window_MouseLeftButtonDown"
        MouseEnter="Window_MouseEnter"
        MouseLeave="Window_MouseLeave">
    
    <!-- Minimal Container - Purely transparent, tiny size -->
    <Border x:Name="MainBorder"
            CornerRadius="4" 
            Background="#B0050508"
            BorderBrush="Transparent"
            BorderThickness="1"
            Padding="4,2">
        <Border.Effect>
            <DropShadowEffect Color="Black" BlurRadius="8" ShadowDepth="1" Opacity="0.4"/>
        </Border.Effect>
        
        <Grid>
            <!-- Close Button - Only visible on hover, positioned at top-right -->
            <TextBlock x:Name="CloseButton"
                       Text="✕" 
                       FontSize="8" 
                       Foreground="#80FFFFFF"
                       HorizontalAlignment="Right"
                       VerticalAlignment="Top"
                       Margin="0,-1,-2,0"
                       Cursor="Hand"
                       Visibility="Hidden"
                       MouseLeftButtonDown="CloseButton_Click"
                       ToolTip="Close application"/>
            
            <!-- Download Speed with WiFi icon - Green, compact -->
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="0,0,8,0">
                <TextBlock Text="📶" FontSize="9" VerticalAlignment="Center" Margin="0,0,3,0"/>
                <TextBlock x:Name="DownloadSpeedText"
                           Text="0 KB/s" 
                           FontSize="10" 
                           FontWeight="SemiBold"
                           Foreground="#4ADE80"
                           VerticalAlignment="Center"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
