<Window x:Class="Throughput.Windows.MinimalWidget"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Throughput"
        SizeToContent="WidthAndHeight"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        Cursor="SizeAll"
        MouseLeftButtonDown="Window_MouseLeftButtonDown"
        MouseEnter="Window_MouseEnter"
        MouseLeave="Window_MouseLeave">
    
    <!-- Minimal Container - Purely transparent, tiny size -->
    <Border x:Name="MainBorder"
            CornerRadius="4" 
            Background="#20000000"
            BorderBrush="Transparent"
            BorderThickness="1"
            Padding="4,2">
        
        <Grid>
            <!-- Close Button - Only visible on hover, positioned at top-right -->
            <TextBlock x:Name="CloseButton"
                       Text="✕" 
                       FontSize="8" 
                       Foreground="#80FFFFFF"
                       HorizontalAlignment="Right"
                       VerticalAlignment="Top"
                       Margin="0,-1,-2,0"
                       Cursor="Hand"
                       Visibility="Hidden"
                       MouseLeftButtonDown="CloseButton_Click"
                       ToolTip="Close application"/>
            
            <!-- Download Speed Only - Green, compact -->
            <TextBlock x:Name="DownloadSpeedText"
                       Text="0 KB/s" 
                       FontSize="10" 
                       FontWeight="SemiBold"
                       Foreground="#4ADE80"
                       VerticalAlignment="Center"
                       HorizontalAlignment="Center"
                       Margin="0,0,8,0"/>
        </Grid>
    </Border>
</Window>
